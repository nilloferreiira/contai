# .env.example (tracked, placeholders)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/contai
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000

# Optional, for testing on a phone/device over LAN with `pnpm dev` — leave
# unset otherwise. Never set TRUST_REQUEST_HOST in a real deployment.
# DEV_ALLOWED_ORIGIN=192.168.x.x
# TRUST_REQUEST_HOST=true
